# Пример файла переменных окружения для Docker Compose
# Скопируйте содержимое этого файла в файл .env и измените значения

# PostgreSQL
POSTGRES_PASSWORD=SecurePassword123!
POSTGRES_USER=cargo_user
POSTGRES_DB=cargo_db

# pgAdmin
PGADMIN_EMAIL=admin@cargo.local
PGADMIN_PASSWORD=admin

# Application Settings
ASPNETCORE_ENVIRONMENT=Production

# JWT Settings (для будущего использования)
# JWT_SECRET=your-super-secret-jwt-key-min-32-chars
# JWT_ISSUER=https://cargo.yourdomain.com
# JWT_AUDIENCE=https://cargo.yourdomain.com

# Cors Settings
# CORS_ORIGINS=https://app.yourdomain.com,https://admin.yourdomain.com


