# 🔄 Как изменить роль пользователя

## Вернуть из Manager обратно в Client

### **Способ 1: Через команду бота (РЕКОМЕНДУЕТСЯ)**

Отправь боту команду:
```
/removeManager <твой_telegram_id>
```

**Пример:**
```
/removeManager 6698369098
```

**Требования:**
- Ты должен быть SystemAdmin (через `Admin__TelegramIds` или роль в БД)
- Пользователь должен существовать в БД (залогиниться хотя бы раз)

---

### **Способ 2: Через SQL (если команда не работает)**

Подключись к PostgreSQL на Railway и выполни:

```sql
-- Вернуть роль Client
UPDATE "AspNetUsers" 
SET "Role" = 2  -- Client = 2
WHERE "TelegramId" = 6698369098;
```

**Проверка:**
```sql
-- Проверить текущую роль
SELECT "TelegramId", "FirstName", "Role" 
FROM "AspNetUsers" 
WHERE "TelegramId" = 6698369098;
```

---

## Другие роли

### **Стать SystemAdmin:**
```sql
UPDATE "AspNetUsers" 
SET "Role" = 0  -- SystemAdmin = 0
WHERE "TelegramId" = твой_telegram_id;
```

### **Стать Manager:**
```sql
UPDATE "AspNetUsers" 
SET "Role" = 1  -- Manager = 1
WHERE "TelegramId" = твой_telegram_id;
```

Или через команду бота:
```
/createManager <telegram_id>
```

---

## Значения ролей

```csharp
SystemAdmin = 0
Manager = 1
Client = 2
```

---

## После изменения роли

1. **Выйди из приложения** (если залогинен)
2. **Залогинься снова** - новая роль применится
3. **Проверь доступ:**
   - Client → видит только свои треки через Telegram WebApp
   - Manager → видит все треки через Manager Dashboard
   - SystemAdmin → полный доступ + команды бота
