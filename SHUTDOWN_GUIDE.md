# 🛑 Как правильно остановить все процессы

> Инструкция по безопасной остановке всех сервисов проекта Cargo.Solution

---

## 📋 Что у тебя сейчас запущено

### **☁️ На Railway (Cloud):**
1. **Backend API** (cargo-saas-production)
2. **Frontend WebApp** (gentle-comfort-production-da23)
3. **PostgreSQL Database** (автоматический сервис)

### **💻 Локально (твой Mac):**
1. **Docker Desktop** (может быть запущен)
2. **Terminal сессии** (если что-то запускали локально)

---

## 🌐 Railway - Как управлять сервисами

### **Вариант 1: Полная остановка (рекомендуется для экономии)**

Если хочешь **полностью остановить** проект на Railway:

```bash
# 1. Зайди в Railway Dashboard
open https://railway.app/dashboard

# 2. Выбери проект "CargoSaas"

# 3. Для каждого сервиса (Backend, Frontend):
   - Открой сервис
   - Settings → Danger Zone → "Remove Service" (или "Pause")

# 4. PostgreSQL базу можно оставить - она занимает мало места
```

**⚠️ ВАЖНО:** 
- Railway **бесплатный tier** дает $5 кредита в месяц
- Если не планируешь использовать сегодня - лучше удалить сервисы
- База данных сохранится, данные не потеряются

---

### **Вариант 2: Временная пауза (если вернешься скоро)**

Railway не имеет кнопки "Pause", но можно:

1. **Удалить environment variables**  
   → Сервис крашнется и не будет потреблять ресурсы

2. **Уменьшить количество реплик до 0**  
   → В Settings → Deploy → Replicas → 0

---

### **Вариант 3: Оставить работающим (если хочешь показать клиенту)**

Ничего не делай! Сервисы будут работать 24/7 на твоем Railway аккаунте.

**Мониторинг расходов:**
```
Railway Dashboard → Settings → Usage
```

---

## 🐳 Docker Desktop - Остановка локальных контейнеров

### **Проверь что запущено:**

```bash
docker ps
```

**Если видишь контейнеры с `cargo-saas`:**

```bash
# Остановить все контейнеры
docker stop $(docker ps -q)

# Или конкретный контейнер
docker stop cargo-api
docker stop cargo-postgres

# Удалить контейнеры (опционально)
docker rm $(docker ps -aq)

# Удалить volumes (опционально, УДАЛИТ данные БД!)
docker volume prune
```

### **Полностью выключить Docker Desktop:**

```bash
# Через GUI:
1. Кликни на иконку Docker в menu bar (вверху справа)
2. Quit Docker Desktop

# Или через терминал:
osascript -e 'quit app "Docker"'
```

---

## 🔌 Терминалы - Закрытие фоновых процессов

### **Проверь запущенные процессы .NET:**

```bash
# Найти все процессы dotnet
ps aux | grep dotnet

# Убить все процессы dotnet (если что-то зависло)
pkill -f dotnet
```

### **Проверь запущенные Node.js процессы:**

```bash
# Найти все процессы node
ps aux | grep node

# Убить все процессы node
pkill -f node
```

### **Проверь используемые порты:**

```bash
# Проверить кто слушает порты 8080, 5173, 5432
lsof -i :8080
lsof -i :5173  # Vite dev server
lsof -i :5432  # PostgreSQL

# Убить процесс на конкретном порту (если зависло)
kill -9 $(lsof -t -i :8080)
```

---

## 🤖 Telegram Bot - Остановка

### **Бот работает на Railway в Backend:**

Если Backend остановлен → Бот автоматически остановлен.

### **Сбросить Webhook (если планируешь переключиться на Webhook):**

```bash
curl https://api.telegram.org/bot<ТВО_BOT_TOKEN>/deleteWebhook?drop_pending_updates=true
```

### **Проверить статус бота:**

```bash
curl https://api.telegram.org/bot<ТВО_BOT_TOKEN>/getMe
```

---

## 🗄️ PostgreSQL - Управление базой данных

### **База на Railway:**

```bash
# Зайди в Railway Dashboard
# Выбери PostgreSQL сервис
# Settings → "Remove Service" (если хочешь полностью удалить)
```

**⚠️ ОСТОРОЖНО:** Удаление PostgreSQL сервиса **удалит все данные!**

### **Локальная база (Docker):**

```bash
# Остановить контейнер (данные сохранятся)
docker stop cargo-postgres

# Удалить контейнер (данные сохранятся в volume)
docker rm cargo-postgres

# Удалить volume (УДАЛИТ все данные!)
docker volume rm cargo-saas_postgres-data
```

---

## 📊 Чек-лист перед выключением

Пройдись по этому списку:

- [ ] **Railway:** Сервисы удалены или на паузе
- [ ] **Docker Desktop:** Контейнеры остановлены
- [ ] **Docker Desktop:** Приложение закрыто (освободит 2-3 GB RAM)
- [ ] **Terminal:** Нет запущенных `dotnet` процессов
- [ ] **Terminal:** Нет запущенных `node` процессов
- [ ] **Порты:** 8080, 5173, 5432 свободны
- [ ] **Git:** Все изменения закоммичены и запушены

---

## 🔄 Как возобновить работу завтра

### **Railway:**

1. **Re-deploy сервисы:**
   ```bash
   # Просто сделай новый push в GitHub
   git push origin main
   
   # Railway автоматически задеплоит
   ```

2. **Или восстанови через Dashboard:**
   - Railway Dashboard → New Service → Import from GitHub

---

### **Локально:**

```bash
# 1. Запусти Docker Desktop
open -a Docker

# 2. Подожди пока Docker запустится (30 сек)

# 3. Запусти контейнеры
cd /Users/daniillednik/CargoSaas
docker-compose up -d

# 4. Проверь логи
docker-compose logs -f

# 5. Открой приложение
open http://localhost:8080
```

---

## 💰 Экономия ресурсов Railway

### **Текущие расходы (примерно):**

- **Backend API:** ~$2-3/месяц (при постоянной работе)
- **Frontend:** ~$1/месяц
- **PostgreSQL:** ~$1/месяц (512 MB data limit на free tier)

### **Как сэкономить:**

1. **Удаляй сервисы когда не используешь** (данные в PostgreSQL сохранятся)
2. **Оптимизируй Docker образы** (меньше размер = меньше расход)
3. **Используй shared CPU вместо dedicated**
4. **Переключись на Webhooks** (меньше нагрузка на Long Polling)

---

## 🆘 Troubleshooting

### **"Docker не останавливается"**

```bash
# Force quit Docker
sudo killall Docker
sudo killall com.docker.backend
```

---

### **"Порт 8080 занят после остановки"**

```bash
# Найди процесс
lsof -i :8080

# Убей его
kill -9 <PID>
```

---

### **"Railway продолжает начислять расход"**

1. Проверь что **все сервисы удалены**:
   - Railway Dashboard → Project → Delete all services

2. Проверь **нет ли других проектов**:
   - Railway Dashboard → Projects → Delete unused

3. Проверь **Usage & Billing**:
   - Settings → Usage (должно быть $0.00/day)

---

### **"Забыл DATABASE_URL от Railway"**

```bash
# Зайди в Railway Dashboard
# PostgreSQL service → Variables → DATABASE_URL
# Скопируй и сохрани в безопасном месте (например, 1Password)
```

---

## 📝 Рекомендации на будущее

### **1. Используй .env файлы локально:**

Создай файл `.env.local` (уже в `.gitignore`):

```bash
DATABASE_URL=postgresql://...
Jwt__SecretKey=твой_секрет
Telegram__BotToken=твой_токен
Telegram__WebAppUrl=https://...
```

---

### **2. Храни секреты в Password Manager:**

- **1Password** (рекомендую)
- **Bitwarden** (open-source)
- **LastPass**

**Что сохранить:**
- Railway DATABASE_URL
- Telegram Bot Token
- Jwt Secret Key
- Railway API Token (если нужен)

---

### **3. Делай бэкапы PostgreSQL:**

```bash
# Скачай дамп базы через Railway CLI
railway run pg_dump > backup_$(date +%F).sql

# Или через pgAdmin/DBeaver
```

---

### **4. Используй Railway CLI для быстрого управления:**

```bash
# Установи Railway CLI
brew install railway

# Логин
railway login

# Просмотр логов
railway logs

# Выполнить команду в production
railway run bash
```

---

## ✅ Финальный чек-лист

Перед тем как закрыть все:

```bash
# 1. Сохрани все изменения
git status
git add .
git commit -m "work in progress"
git push origin main

# 2. Проверь Railway
open https://railway.app/dashboard

# 3. Останови Docker
docker stop $(docker ps -q)
osascript -e 'quit app "Docker"'

# 4. Закрой терминалы
# (можешь оставить - они не потребляют ресурсы)
```

---

## 🎯 TL;DR - Быстрая остановка

### **Если торопишься:**

```bash
# 1. Останови локальные контейнеры
docker stop $(docker ps -q)

# 2. Закрой Docker Desktop
osascript -e 'quit app "Docker"'

# 3. Railway оставь работать (или зайди и удали сервисы)
open https://railway.app/dashboard
```

---

## 🌙 Спокойной ночи!

**Все твои данные сохранены:**
- ✅ Код в GitHub
- ✅ База данных на Railway (не удаляй PostgreSQL!)
- ✅ Конфигурация в environment variables

**Завтра сможешь продолжить с того же места!** 🚀

---

## 📞 Контакты для помощи

**Документация:**
- [Railway Docs](https://docs.railway.app/)
- [Docker Docs](https://docs.docker.com/)
- [.NET Docs](https://learn.microsoft.com/en-us/dotnet/)

**Если что-то пошло не так:**
- Railway Support: support@railway.app
- GitHub Issues: создай issue в своем репо
- Stack Overflow: тегай `railway`, `.net`, `docker`

---

**🎉 Отличной работы сегодня! Отдыхай!** 🌟

